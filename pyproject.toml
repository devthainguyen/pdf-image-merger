[project]
name = "pdf-image-merger"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "devthainguyen", email = "129784562+devthainguyen@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pillow>=10.0.0",
    "pymupdf>=1.24.0",
    "ruff>=0.16.5",
]

[project.scripts]
pdf-image-merger = "pdf_image_merger:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
